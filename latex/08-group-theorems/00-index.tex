\chapter{Chapter 8: Group examples and basic theorems}\label{sec:08-group-theorems:00-index:1}

\begin{learningobjectives}
\begin{itemize}
\tightlist
\item
  Prove a fact about \emph{every} group at once by reasoning only from the fields of \texttt{Group}.
\item
  Recognize the ``relate both sides to a common third expression'' and ``pad with the identity, then cancel'' patterns.
\item
  Reuse a proved lemma (\texttt{left\_\allowbreak{}inverse\_\allowbreak{}unique}) to shortcut a later one (\texttt{inv\_\allowbreak{}op}) instead of re-deriving it.
\end{itemize}
\end{learningobjectives}


This chapter proves theorems about an \emph{arbitrary} group. We are given some \texttt{Grp : Group G} for an unknown \texttt{G}, and we use only the fields from Chapter 7. Chapter 7 named a payoff it had not yet cashed in, that a theorem proved once, generically, is inherited for free by every group built afterward. This chapter cashes it in, and the point of the chapter lies less in the three theorems themselves (they are standard) than in \textbf{the search process} for finding each proof, given a goal, what to examine, what to attempt, and how to recognize being stuck versus one step away. Each theorem below is presented as that search, not merely its answer.

Stating a theorem about ``every group,'' rather than about one specific group like \texttt{intGroup}, means fixing an arbitrary \texttt{Grp : Group G} with \texttt{variable}, the same way a written proof opens with ``let \(G\) be a group'' (\hyperref[sec:08-group-theorems:01-setup:1]{Section 1}). The first fact worth proving this way is that the identity is unique (\hyperref[sec:08-group-theorems:02-theorem-1:1]{Section 2}), chosen first because its proof introduces the central technique of the chapter, when two opaque things must be shown equal, find a third expression both sides equal on their own. The same ``relate to a common expression'' idea stretches to a harder claim, uniqueness of inverses (\hyperref[sec:08-group-theorems:03-theorem-2:1]{Section 3}), though this time no single axiom hands over the needed third expression directly; it must be built by padding an element with the identity and then swapping the identity for something cancelable. Having just proved that a left inverse is unique, that fact is then reused rather than re-derived: proving \((ab)^{-1} = b^{-1}a^{-1}\) (\hyperref[sec:08-group-theorems:04-theorem-3:1]{Section 4}) reduces to checking that \(b^{-1}a^{-1}\) satisfies the characterizing equation of Theorem 2, a ``compute this'' goal turned into a cheaper ``verify this'' goal, and the result is then applied, with no new proof, to the concrete permutation group of Chapter 7, delivering the ``proved once, free everywhere'' promise for real.

Chapter 9 needs exactly this pattern again, one level up, a second operation added to the structure of a group, with its own theorems to prove generically before any concrete ring is built.


% Auto-appended: input every other section in order.
\input{08-group-theorems/01-setup.tex}
\input{08-group-theorems/02-theorem-1.tex}
\input{08-group-theorems/03-theorem-2.tex}
\input{08-group-theorems/04-theorem-3.tex}
\input{08-group-theorems/05-exercises.tex}
